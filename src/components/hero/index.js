import React from 'react'
import { HeroBtn, HeroHeading, HeroText, HeroWrapper } from './style'

const HeroSection = () => {
  return (
    <HeroWrapper>
        <HeroHeading>Infinite access to Top Talents.</HeroHeading>
        <HeroText>Streamline your recruitment with easy access to top talents around the globe with a single click</HeroText>
        <HeroBtn>Get Started</HeroBtn>
    </HeroWrapper>
  )
}

export default HeroSection